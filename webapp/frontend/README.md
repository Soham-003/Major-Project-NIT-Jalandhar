Frontend skeleton (React + Tailwind + Chart.js)

Install and run:

```bash
cd webapp/frontend
npm install
npm start
```

The UI connects to the backend at `http://127.0.0.1:5050` and provides:

* landing page hero
* prediction form with manual input and CSV upload
* live EEG simulation charts
* model performance visualizations
* explainability analytics
